<!DOCTYPE html>
<html>
  <head>
    <title>MembersOnly</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <nav class="navbar navbar-light bg-light justify-content-between">
  <%= link_to "Members Only", root_path, class: 'justify-content-start navbar-brand' %>
  
  <% if @user %>
                  <button class="btn btn-danger"><%= link_to "Log out", logout_path, method: :delete, class: 'white' %></button>
                  <% else %>
                  <button class="btn btn-info"><%= link_to "Log in", login_path, class: 'white' %></button>
              <% end %>
</nav>
  <div class="container">
      <% flash.each do |message_type, message| %>
        <%= content_tag(:div, message, class: "alert alert-#{message_type}") %>
      <% end %>
    <%= yield %>
  </body>
</html>
